apiVersion: v1
kind: Pod
metadata:
 name: pod-pvc2
spec:
 containers:
  - image: nginx
    name: c2
    volumeMounts:
     - mountPath: "/data"
       name: my-volume
 volumes:
  - name: my-volume
    persistentVolumeClaim:
     claimName: sc-pvc
